{
  "name": "enlightened-structures",
  "version": "0.0.1",
  "description": "",
  "main": "server.js",
  "scripts": {
    "build": "./node_modules/.bin/harp compile src dist",
    "dev": "npm start & browser-sync start --proxy 'localhost:9000' --files 'src/*.jade, src/styles/*, src/img/*'",
    "h2j": "./node_modules/.bin/html2jade",
    "shipit": "npm test && git push origin head",
    "start": "./node_modules/.bin/harp server --port ${PORT:=9000} src",
    "test": "npm run build"
  },
  "author": "Harlan T Wood",
  "license": "",
  "devDependencies": {
    "html2jade": "^0.8.3",
    "browser-sync": "^1.3.7"
  },
  "dependencies": {
    "harp": "^0.13.0"
  },
  "engines": {
    "node": "~0.10.38",
    "npm": "^2.7.6"
  }
}
